pipeline {
  agent any
    
  tools {nodejs "nodejs"}
    
  stages {
        
    stage('Build') {
      steps {
        dir('backend'){
          sh 'npm install'
          sh '<<Build Command>>'
        }
      }
    }  

    stage('Test') {
      steps {
        sh 'node test'
      }
    }
  }
}